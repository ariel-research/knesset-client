/**
 * Find all elements in array that match a given prefix.
 * @param {string} prefix 
 * @param {string} arr 
 * @returns {Array} all matches
 */
const findMatchesToPrefix = (prefix, arr) => {
    let ans = [];
    return ans;
}

module.exports = findMatchesToPrefix;